# 腾讯云静态网站托管部署详细步骤

## 第一步：开通腾讯云静态网站托管服务

### 1. 登录腾讯云控制台
- 访问：https://console.cloud.tencent.com/
- 使用您刚注册的账号登录

### 2. 开通静态网站托管服务
- 在控制台搜索"静态网站托管"
- 选择"Website Hosting"服务
- 点击"立即使用"
- 选择"免费额度"（每月50GB流量，10GB存储）

### 3. 创建存储桶（Bucket）
- 存储桶名称：`professional-prompt-generator`（建议）
- 地域选择：`ap-guangzhou`（广州）或离您最近的区域
- 访问权限：选择"公有读私有写"
- 静态网站配置：启用"静态网站"

## 第二步：获取API密钥

### 1. 获取SecretId和SecretKey
- 进入"访问管理" → "API密钥管理"
- 点击"新建密钥"
- 复制保存 `SecretId` 和 `SecretKey`（重要！）

## 第三步：配置GitHub Secrets

### 1. 进入GitHub仓库设置
- 访问您的GitHub仓库：https://github.com/Jackypeng1207/professional-prompt-generator
- 点击"Settings" → "Secrets and variables" → "Actions"

### 2. 添加两个Secret
- `TENCENT_SECRET_ID`：粘贴您的SecretId
- `TENCENT_SECRET_KEY`：粘贴您的SecretKey

## 第四步：创建GitHub Actions工作流

### 1. 创建配置文件
我已经为您创建了 `.github/workflows/deploy.yml` 文件

### 2. 修改配置（如果需要）
- 打开 `tencent-cloud-deploy.yml` 文件
- 修改 `bucket` 为您的存储桶名称
- 修改 `region` 为您的区域

## 第五步：推送代码触发部署

### 1. 提交配置文件
```bash
git add .
git commit -m "feat: 添加腾讯云部署配置"
git push origin main
```

### 2. 查看部署状态
- 进入GitHub仓库的"Actions"标签页
- 查看部署工作流状态

## 第六步：访问您的网站

### 1. 获取访问地址
- 返回腾讯云控制台
- 进入静态网站托管服务
- 查看"默认域名"（格式：`bucket-name.cos-website.region.myqcloud.com`）

### 2. 测试访问
- 复制默认域名
- 在浏览器中打开测试

## 自定义域名配置（可选）

### 1. 备案域名
- 如果您有备案的域名
- 在腾讯云控制台配置自定义域名

### 2. CDN加速
- 开启CDN加速提高访问速度
- 配置SSL证书启用HTTPS

## 故障排除

### 常见问题
1. **部署失败**：检查SecretId和SecretKey是否正确
2. **访问403**：检查存储桶权限是否为"公有读"
3. **页面空白**：检查构建是否成功，dist目录是否有index.html

### 技术支持
- 腾讯云文档：https://cloud.tencent.com/document/product/436/9512
- GitHub Actions文档：https://docs.github.com/en/actions

## 部署完成后的链接
您的网站将可以通过以下地址访问：
- 默认地址：`https://professional-prompt-generator-xxxxxxxx.cos-website.ap-guangzhou.myqcloud.com`
- 自定义域名（配置后）：`https://your-domain.com`

## 自动更新
配置完成后，每次您推送代码到main分支，GitHub Actions会自动：
1. 构建项目
2. 部署到腾讯云
3. 更新网站内容

---

**开始部署吧！如果有任何问题，我可以帮您解决。**