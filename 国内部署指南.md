# 国内部署指南

## 问题分析
Vercel部署的网站对国内用户存在访问限制，主要问题包括：
- DNS污染导致无法访问
- 网络环境限制境外服务
- 访问速度不稳定

## 解决方案

### 方案1：腾讯云静态网站托管（推荐）
**优势**：
- 国内服务器，访问速度快
- 支持自动部署
- 免费额度充足

**部署步骤**：
1. 注册腾讯云账号
2. 开通静态网站托管服务
3. 配置自动部署
4. 绑定自定义域名（可选）

### 方案2：阿里云OSS + CDN
**优势**：
- 成本低廉
- 全球加速
- 稳定性高

**部署步骤**：
1. 创建OSS存储桶
2. 配置静态网站托管
3. 绑定CDN加速
4. 设置域名解析

### 方案3：Coding Pages
**优势**：
- 国内访问速度快
- 与GitHub类似的工作流
- 免费使用

## 立即解决方案

### 1. 本地构建分发
```bash
npm run build
# 将dist目录打包分享
```

### 2. 开发环境演示
- 链接：http://localhost:5173/
- 适合内部测试和演示

## 部署配置文件示例

### Docker部署配置
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npx", "serve", "-s", "dist", "-l", "3000"]
```

### Nginx配置
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }
}
```

## 最佳实践建议

1. **优先选择国内云服务商**：腾讯云、阿里云
2. **使用CDN加速**：提高访问速度
3. **备案域名**：提供正式服务
4. **监控访问状态**：确保服务可用性

## 紧急联系方式
如遇部署问题，可联系技术支持或参考相关文档。